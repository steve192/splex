{
  "name": "splex-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing landing page for Splex, served by the backend at the site root.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "dev": "npm run sync-assets && astro dev",
    "build": "npm run sync-assets && astro build",
    "preview": "astro preview",
    "test": "node scripts/check-built-pages.mjs",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@fontsource-variable/inter": "5.3.0",
    "@tailwindcss/vite": "4.3.3",
    "astro": "7.2.9",
    "sharp": "0.35.4",
    "tailwindcss": "4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "7.0.2"
  }
}
